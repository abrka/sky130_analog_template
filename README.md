# How To Use

## MOS Model and Helper Scripts
- `scripts/mos_model.py`contains useful variables
- `scripts/calc_wl.py` can be used to calculate W/L of transistor

## Schematic Capture
- Go to `schematic` directory
- Include `TT_MODELS` device in your schematic for sky130nm pdk. This file can be found by running xschem on `schematic` directory without any input file
- Create the core circuit with only ipin and opin. Do *not* use `VDD` or `GND` device from xschem
- Create the symbol for the core circuit
- Create the testbench using the symbol normally

## Layout
- Create the layout normally

## LVS
- Set `Simulation>Use simulation dir in schematic dir`
- Set `Simulation>LVS>Top Level as Subcircuit` in xschem
- Generate the netlist for the circuit
- Go to `layout` directory
- Open the magic layout and run the following command in the tcl console
`lvs`
- Next run the following command from project root directory
`sh ./tech_scripts/lvs.sh ${design_name}` where `${design_name}` is the filename of your circuit/layout

## Parasitic Extraction
- Go to `layout` directory
- Open the magic layout and run the following command in the tcl console
`parex`
- Open the generated spice file (`layout/${design_name}_parex.spice`) and order the subcircuit pins in the same order as the netlist generated by xschem (`schematic/simulation/${design_name}.spice`)

## Creating Symbol for Circuit with Parasitics
- In xschem, copy the symbol of the core circuit and create a new symbol for the circuit with parasitcs included
- Open the symbol (`i` if you are in schematic view) and press `q` to open properties
- Change the line `type=subcircuit` to `type=primitive`

## Post Layout Simulation
- Set `Simulation>Use simulation dir in schematic dir`
- Use the previously created symbol with parasitics included in your schematic
- Create a `code` device and add the following to the `value` property of code
  `.include ../../layout/${design_name}_parex.spice` where `${project_dir}` is the directory of your project relative to root
- Simulate normally
